# 📋 Giao việc — Ngày 03/08/2026

| | |
|---|---|
| **Ngày giao** | Thứ Hai, 03/08/2026 |
| **Hạn nộp PR** | Trước 23:00 ngày 04/08/2026 |
| **Thời lượng mỗi task** | 2–4 giờ |
| **Người giao** | Bùi Mậu Văn (Scrum Master) |
| **Tổng** | 8 task · 32 SP · mỗi người 1 task |

---

# ⚠️ PHẦN 1 — QUY TẮC BẮT BUỘC (đọc 5 phút, đọc một lần dùng cả sprint)

## 1.1 Quy tắc quan trọng nhất: mỗi người một vùng file

8 task dưới đây được chia sao cho **không ai sửa file của ai**. Cứ làm song song, không phải chờ nhau, không thể conflict.

**Chỉ có ĐÚNG BA file dùng chung:**

| File | Ai đụng | Đụng bao nhiêu |
|---|---|---|
| `frontend/src/App.tsx` | Quang, Dũng | mỗi người thêm 1–2 dòng `<Route>` |
| `backend/app/api/v1/router.py` | Trường | thêm 1 dòng `include_router` |
| `docs/design/01-requirement-analysis.md` §10 | ai xong thì tick | 1 ô trong bảng |

Nếu bị conflict ở ba file này thì **giữ cả hai bên** — không ai xoá dòng của ai. Conflict 1 dòng, xử lý 30 giây.

## 1.2 Chỉ MỘT người được tạo migration hôm nay

Hôm nay chỉ **Nguyễn Đăng Trường** chạy `alembic revision`.

> **Vì sao:** hai người cùng tạo revision sẽ sinh ra hai "head" trong cây migration. Người thứ ba kéo code về chạy `alembic upgrade head` sẽ nổ lỗi `Multiple heads`, và phải có người ngồi viết merge revision để gỡ. Ai thấy task của mình cần thêm cột — **nhắn nhóm trưởng trước, đừng tự tạo**.

## 1.3 Chưa cần hoàn hảo

Code chạy được, test pass, import không lỗi là đủ. **Đừng ngồi 5 tiếng để làm cho đẹp — cứ commit rồi đi tiếp.** Nhóm trưởng review và sửa tiếp sau khi merge.

Bí thì commit phần làm được, đặt tiêu đề PR bắt đầu bằng `WIP:` và nhắn nhóm. Commit dở còn hơn không có commit.

## 1.4 Sáu bước Git

```bash
# 1. LUÔN làm bước này đầu tiên — bỏ qua là nguyên nhân số một gây conflict
git checkout develop && git pull origin develop

# 2. Tạo nhánh riêng (tên nhánh ghi sẵn trong task của bạn)
git checkout -b <tên-nhánh-của-bạn>

# 3. ... viết code ...

# 4. Commit nhỏ, mỗi commit một ý — đừng `git add .` một cục cuối ngày
git add <đường-dẫn-file-cụ-thể>
git commit -m "<nội dung ghi sẵn trong task của bạn>"

# 5. Đẩy lên GitHub (nên push mỗi ngày kể cả khi chưa xong)
git push -u origin <tên-nhánh-của-bạn>

# 6. Vào github.com/BuiVannn/Smart-IT-Helpdesk → tab "Pull requests"
#    → "New pull request" → base: develop ← compare: nhánh của bạn
```

Sau khi PR được duyệt: bấm **Squash and merge** → **Delete branch**, rồi dọn máy mình:

```bash
git checkout develop && git pull origin develop && git branch -d <nhánh-của-bạn>
```

## 1.5 Đặt tên

**Nhánh** — không dấu, chữ thường, nối bằng gạch ngang. Tên nhánh đã ghi sẵn trong từng task, cứ copy.

**Commit** — theo Conventional Commits, nội dung đã soạn sẵn trong từng task:

```
<loại>(<phạm vi>): <mô tả ngắn, không viết hoa đầu, không chấm cuối>
```

Loại: `feat` (tính năng mới) · `fix` (sửa lỗi) · `test` · `docs` · `chore` (CI, cấu hình) · `refactor` · `perf`.

## 1.6 Review

**Người review mọi PR: Bùi Mậu Văn.** Gán `BuiVannn` vào ô *Reviewers*, gán chính mình vào ô *Assignees*. Không gán chéo cho nhau.

## 1.7 Trước khi mở PR, tự kiểm ba việc

```bash
cd backend
.venv-tools/bin/pytest tests/ -q          # không được có test đỏ
```

> **Lưu ý:** `.venv-tools` hiện **chưa có** `ruff`, `black`, `mypy`. Muốn kiểm tra định dạng ngay trên máy thì cài thêm một lần:
>
> ```bash
> .venv-tools/bin/pip install ruff black mypy import-linter
> .venv-tools/bin/ruff check . && .venv-tools/bin/black --check .
> ```
>
> Không cài cũng được — CI vẫn kiểm. Nhưng cài thì biết sớm hơn 3 phút thay vì chờ CI chạy xong.

Với người làm frontend:

```bash
cd frontend && npm run typecheck && npm run build
```

**Tuyệt đối không commit `.env`, API key, mật khẩu, hay dữ liệu thật.** Repo có gitleaks quét toàn bộ lịch sử — lộ key là PR đỏ và phải xoá lịch sử để gỡ.

## 1.8 Chạy dự án trên máy

```bash
cp .env.example .env          # rồi điền các giá trị được nhóm trưởng gửi riêng
docker compose up -d postgres redis api worker beat
```

> **`worker` và `beat` là bắt buộc, không được bỏ.** Phân loại AI, index tài liệu và nhắc SLA đều chạy nền qua Celery. Thiếu `worker`, ticket tạo ra sẽ mãi không được phân loại — nhìn y hệt như AI bị hỏng, và sẽ mất cả buổi đi tìm lỗi không tồn tại.

| | |
|---|---|
| API + Swagger | http://localhost:8000/docs |
| Frontend | http://localhost:5173 |
| PostgreSQL | `localhost:5434` |

Có tài liệu mẫu cho chatbot:

```bash
cd backend
.venv-tools/bin/python scripts/seed.py            # người dùng, phòng ban, loại sự cố
.venv-tools/bin/python scripts/seed_kb.py         # 20 bài hướng dẫn
.venv-tools/bin/python scripts/reindex_kb.py --all   # index cho chatbot
```

## 1.9 ✅ `develop` đã xanh — bắt đầu được ngay

Điều kiện tiên quyết đã xong. `fix/p0-nghiep-vu` đã merge vào `develop` qua PR #19, #20, #22 — đỉnh hiện tại là `f031bc8`.

Trạng thái đã được kiểm chứng trên **database trắng, `TZ=UTC`**, đúng môi trường CI:

```
565 passed, 19 skipped, exit 0
```

Test từng làm đỏ CI (`test_sap_xep_theo_ten_dao_chieu_dung`) đã được sửa: nó tự tạo ba tài khoản có dấu riêng rồi lọc theo dấu đó, thay vì trông chờ vào dữ liệu seed sẵn có trên máy cá nhân.

> **19 test `skipped` là chuyện đã biết**, không phải lỗi mới — đó là toàn bộ test chatbot, bị bỏ qua vì kho tài liệu chưa được index. Task số 1️⃣ của Cao Mạnh Hà chính là đóng lỗ hổng này.

**Việc đầu tiên của mọi người sáng nay:**

```bash
git checkout develop && git pull origin develop
```

---

# 📌 PHẦN 2 — BẢNG TỔNG HỢP

| # | Người | Task | SP | Nhánh |
|---|---|---|---|---|
| 1 | **Cao Mạnh Hà** | Siết cổng chất lượng CI | 3 | `chore/ci-siet-cong-chat-luong` |
| 2 | **Nguyễn Văn Quang** | Giao diện Kho tài liệu `/kb` | 8 | `feature/F5-kb-ui` |
| 3 | **Nguyễn Đăng Trường** | API đánh giá sau xử lý (US-41) | 3 | `feature/F8-danh-gia-api` |
| 4 | **Trần Quang Ngọc** | 2 test phân quyền bắt buộc (US-06) | 3 | `test/US-06-ma-tran-phan-quyen` |
| 5 | **Nguyễn Văn Dũng** | Trang Đăng ký + Đổi mật khẩu | 5 | `feature/US-01-US-05-ui` |
| 6 | **Chu Quang Vũ** | Sửa/xoá bình luận (US-15) | 3 | `feature/US-15-sua-xoa-binh-luan` |
| 7 | **Nguyễn Tiến Lưỡng** | Báo cáo câu hỏi chatbot trả lời kém (US-27) | 5 | `feature/US-27-cau-hoi-tra-loi-kem` |
| 8 | **Lại Duy Đông** | Sửa mã lỗi đổi mật khẩu + audit (US-05) | 2 | `fix/US-05-ma-loi-va-audit` |

---

# 1️⃣ Cao Mạnh Hà — Siết cổng chất lượng CI

| | |
|---|---|
| **Nhánh** | `chore/ci-siet-cong-chat-luong` |
| **File sửa** | `.github/workflows/ci.yml` |
| **File tạo mới** | `backend/uv.lock` |
| **Thời gian** | ~2 giờ |
| **SP** | 3 |

## Bối cảnh

CI hiện đang **xanh hơn thực tế**. Bốn chỗ đang bỏ lọt lỗi:

1. `mypy app || true` — dấu `|| true` nuốt mọi lỗi kiểu. Cổng này chưa bao giờ chặn được gì.
2. CI chỉ quét `tests/unit`, `tests/integration`, `tests/api` ⇒ file `backend/tests/test_app_boots_standalone.py` nằm ở thư mục `tests/` gốc **chưa từng chạy một lần nào**.
3. **19 test chatbot tự bỏ qua trên CI** với lý do `Chưa có chunk nào — chạy scripts/reindex_kb.py --all trước`. CI không có bước nạp kho tài liệu, nên toàn bộ F4 (chatbot RAG) chưa từng được kiểm chứng.
4. Không có `uv.lock`. CI giải phụ thuộc lại từ đầu mỗi lần ⇒ một thư viện lên phiên bản mới là CI đỏ dù không ai đụng vào code.

## Việc cần làm

**a. Bỏ `|| true` khỏi bước type check**

```yaml
      - name: Type check
        run: mypy app          # bỏ "|| true"
```

Nếu `mypy` báo lỗi có sẵn, đừng sửa code người khác — thêm `# type: ignore[<mã-lỗi>]` kèm chú thích ngắn, hoặc liệt kê module vào `[[tool.mypy.overrides]]` trong `pyproject.toml` và nhắn nhóm.

**b. Thêm bước nạp và index kho tài liệu, đặt NGAY SAU bước `Run migrations`**

```yaml
      - name: Seed dữ liệu và index kho tài liệu
        run: |
          python scripts/seed.py
          python scripts/seed_kb.py
          python scripts/reindex_kb.py --all
```

**c. Đổi đường quét test** — hai bước pytest hiện tại gộp lại còn:

```yaml
      - name: Unit tests
        run: pytest tests/unit -v --cov=app --cov-report=

      - name: Integration and API tests
        run: >-
          pytest tests/integration tests/api tests/test_app_boots_standalone.py -v
          --cov=app --cov-append --cov-report=term-missing
          --cov-fail-under=80
```

**d. Sinh và commit `uv.lock`**

```bash
cd backend && uv lock
git add uv.lock
```

Rồi đổi bước cài đặt trong `ci.yml`:

```yaml
      - name: Install dependencies
        run: uv sync --frozen --all-groups
```

## Cách tự kiểm tra

Sau khi push, vào tab **Actions** trên GitHub và xác nhận:

- Log của bước "Integration and API tests" hiển thị **`skipped` giảm từ 19 xuống dưới 5**
- Bước "Type check" thật sự chạy mypy và không có `|| true`
- Tổng số test **tăng lên** so với lần chạy trước

## Commit

```bash
git add .github/workflows/ci.yml backend/uv.lock
git commit -m "chore(ci): siet cong chat luong, khoa phien ban phu thuoc

Bo '|| true' sau mypy — cong type check truoc gio khong chan duoc gi.
Them buoc seed va index kho tai lieu: 19 test chatbot dang tu bo qua tren
CI vi khong co chunk nao, nghia la toan bo F4 chua tung duoc kiem chung.
Quet ca tests/test_app_boots_standalone.py — file nay nam ngoai duong quet
cu nen chua chay lan nao. Nang --cov-fail-under tu 70 len 80 (thuc te 85,7%).
Them uv.lock de CI va may ca nhan dung dung mot bo phien ban."
```

## Hoàn thành khi

- [ ] CI xanh, và số test chạy **nhiều hơn** lần trước
- [ ] Số test `skipped` giảm rõ rệt
- [ ] `uv.lock` có trong repo

---

# 2️⃣ Nguyễn Văn Quang — Giao diện Kho tài liệu

| | |
|---|---|
| **Nhánh** | `feature/F5-kb-ui` |
| **File tạo mới** | `frontend/src/api/kb.ts`, `frontend/src/features/knowledge/*` |
| **File sửa** | `frontend/src/App.tsx` (1 dòng), `frontend/src/types/index.ts` |
| **File mẫu để copy** | `frontend/src/features/admin/UsersPage.tsx` + `frontend/src/api/admin.ts` ← **mở hai file này trước** |
| **Thời gian** | ~4 giờ |
| **SP** | 8 |

## Bối cảnh

Backend F5 **đã xong hoàn toàn** — 10 endpoint chạy tốt, có test. Nhưng route `/kb` trên giao diện đang là:

```tsx
<Route path="/kb" element={<Placeholder title="Tài liệu hướng dẫn" />} />
```

18 SP công sức backend đang nằm im sau chữ "Placeholder", và đó là mục nằm ngay trên thanh điều hướng chính — người chấm sẽ bấm vào đầu tiên.

Ngoài ra đây còn là **đường duy nhất để nuôi chatbot**: hiện muốn thêm tài liệu cho AI phải mở terminal chạy script. Có trang này thì vòng lặp "thiếu tài liệu → viết bài → chatbot trả lời được" mới khép lại.

## API có sẵn (đã chạy, xem Swagger tại `/docs`)

| Method | Path | Dùng để |
|---|---|---|
| `GET` | `/kb/articles?q=&categoryId=&tag=&status=&page=&pageSize=` | Danh sách + tìm kiếm |
| `GET` | `/kb/articles/{slug}` | Xem chi tiết |
| `POST` | `/kb/articles` | Tạo bài mới |
| `PATCH` | `/kb/articles/{id}` | Sửa bài |
| `POST` | `/kb/articles/{id}/publish` | Đăng bài |
| `POST` | `/kb/articles/{id}/unpublish` | Gỡ bài |
| `GET` | `/kb/categories` | Danh sách chủ đề |

Hình dạng dữ liệu trả về (`ArticleListItem`):

```ts
{ id, slug, title, summary, status, category, tags,
  viewCount, publishedAt, updatedAt, version }
```

Bài chi tiết có thêm `contentMd` (Markdown) và `author`.

> ⚠️ **Tham số `status` chỉ Admin dùng được — vai trò khác luôn chỉ thấy bài `PUBLISHED`.** Đăng nhập bằng tài khoản `IT_AGENT` sẽ không thấy bài nháp của mình, và đó là **đúng theo thiết kế**, không phải lỗi. Đừng mất thời gian đi tìm. Bộ lọc trạng thái trên giao diện chỉ nên hiện với `ADMIN`.

## Việc cần làm

**a. `frontend/src/api/kb.ts`** — copy nguyên cấu trúc của `api/admin.ts`, đổi đường dẫn.

**b. `features/knowledge/KbListPage.tsx`** — danh sách bài viết
- Ô tìm kiếm (gọi lại API khi gõ, có debounce ~300ms)
- Bộ lọc theo chủ đề (dropdown lấy từ `/kb/categories`)
- Mỗi dòng: tiêu đề, tóm tắt, chủ đề, nhãn trạng thái (`DRAFT` xám / `PUBLISHED` xanh)
- Phân trang — copy component phân trang có sẵn ở `UsersPage.tsx`
- Nút "Viết bài mới" (chỉ hiện với `ADMIN` và `IT_AGENT`)

**c. `features/knowledge/KbDetailPage.tsx`** — xem một bài
- Render `contentMd` bằng Markdown. Nếu chưa có thư viện: `npm i react-markdown`
- Hiện chủ đề, nhãn, lượt xem, ngày đăng, tác giả
- Nút "Sửa" và "Đăng"/"Gỡ" (chỉ `ADMIN` và `IT_AGENT`)

**d. `features/knowledge/KbEditorPage.tsx`** — soạn và sửa
- Ô tiêu đề, ô tóm tắt, ô nội dung Markdown (`<textarea>` là đủ, không cần trình soạn thảo giàu tính năng)
- Chọn chủ đề, nhập nhãn
- Nút "Lưu nháp" và "Đăng"

> ⚠️ **Khi sửa bài phải gửi kèm `version`** lấy từ bài đang xem. Backend dùng khoá lạc quan: gửi sai `version` sẽ nhận **409**. Bắt lỗi 409 và hiện thông báo "Bài viết vừa được người khác sửa, hãy tải lại trang".

**e. `App.tsx`** — thay dòng Placeholder bằng:

```tsx
<Route path="/kb" element={<KbListPage />} />
<Route path="/kb/new" element={<KbEditorPage />} />
<Route path="/kb/:slug" element={<KbDetailPage />} />
<Route path="/kb/:slug/edit" element={<KbEditorPage />} />
```

## Cách tự kiểm tra

```bash
cd backend && .venv-tools/bin/python scripts/seed_kb.py    # nạp 20 bài mẫu
cd ../frontend && npm run dev
```

Mở http://localhost:5173/kb — phải thấy 20 bài. Gõ "wifi" vào ô tìm kiếm → danh sách lọc lại. Bấm vào một bài → thấy nội dung Markdown đã render. Đăng nhập bằng tài khoản admin → viết một bài mới → bấm Đăng → mở `/chat` hỏi đúng nội dung bài vừa viết, chatbot phải trả lời được và trích dẫn bài đó.

## Commit

```bash
git add frontend/src/api/kb.ts frontend/src/features/knowledge frontend/src/App.tsx frontend/src/types
git commit -m "feat(kb): giao dien kho tai lieu — danh sach, chi tiet, soan bai

Backend F5 xong tu lau nhung route /kb van la Placeholder, nen 18 SP cong
suc backend khong ai nhin thay duoc. Day cung la duong duy nhat de nap tai
lieu cho chatbot bang giao dien — truoc gio phai chay script trong terminal.

Gui kem version khi sua bai va bat loi 409: backend dung khoa lac quan,
hai nguoi sua cung luc thi nguoi sau phai biet de tai lai, khong duoc de
ghi de len nhau lang le."
```

## Hoàn thành khi

- [ ] Xem được danh sách, tìm kiếm, mở chi tiết
- [ ] Viết được bài mới và đăng lên
- [ ] Bài vừa đăng được chatbot trích dẫn khi hỏi đúng chủ đề
- [ ] `npm run typecheck && npm run build` sạch

---

# 3️⃣ Nguyễn Đăng Trường — API đánh giá sau xử lý (US-41)

| | |
|---|---|
| **Nhánh** | `feature/F8-danh-gia-api` |
| **File tạo mới** | `backend/app/modules/feedback/{schemas,repository,service,router}.py`, `backend/tests/api/test_danh_gia.py` |
| **File sửa** | `backend/app/api/v1/router.py` (1 dòng) |
| **File mẫu để copy** | `backend/app/modules/notifications/` ← **module nhỏ nhất, cấu trúc y hệt** |
| **Thời gian** | ~3 giờ |
| **SP** | 3 |

## Bối cảnh

Module `feedback/` hiện chỉ có `models.py`. Bảng `ticket_ratings` **đã tồn tại trong database** (bạn đã tạo migration từ trước), có sẵn:

- `UNIQUE` trên `ticket_id` — mỗi ticket đúng một đánh giá
- `CHECK score BETWEEN 1 AND 5`
- `agent_id` là **bản sao có chủ đích** của assignee lúc đánh giá
- `TicketRating.is_editable()` — trả `True` trong 24 giờ đầu

Nghĩa là phần khó nhất đã xong. Việc còn lại là bọc API quanh nó.

## Việc cần làm

**a. `schemas.py`**

```python
class CreateRatingRequest(StrictModel):
    score: int = Field(ge=1, le=5)
    comment: str | None = Field(default=None, max_length=1000)

class RatingResponse(ResponseModel):
    id: UUID
    ticketId: UUID
    score: int
    comment: str | None
    createdAt: datetime
    isEditable: bool
```

**b. `service.py`** — bốn quy tắc, mỗi quy tắc một ngoại lệ:

| Tình huống | Ném |
|---|---|
| Ticket không ở `RESOLVED` hoặc `CLOSED` | `ValidationError` → 422 |
| Người gọi không phải người tạo ticket | `NotFoundError` → **404, không phải 403** |
| Ticket đã có đánh giá | `ConflictError` → 409 |
| Sửa sau 24 giờ | `ForbiddenError` → 403 |

> ⚠️ **Vì sao 404 chứ không phải 403** ở dòng thứ hai: trả 403 là gián tiếp xác nhận "ticket này có thật, chỉ là bạn không được xem". Người ngoài dò ID sẽ đếm được số ticket trong hệ thống. Quy tắc BR-09 của dự án: **không tiết lộ sự tồn tại**. Xem cách `chatbot/service.py::_get_session` làm.

Khi tạo đánh giá, nhớ **chép `ticket.assignee_id` vào `agent_id`** — báo cáo hài lòng phải phản ánh người thực sự xử lý lúc đó, ticket giao lại về sau không được làm đổi báo cáo lịch sử.

**c. `router.py`** — mount **riêng**, không sửa `tickets/router.py`:

```python
router = APIRouter()

@router.post("/{ticket_id}/rating", status_code=201)
@router.get("/{ticket_id}/rating")
@router.patch("/{ticket_id}/rating")
```

Rồi trong `app/api/v1/router.py` thêm đúng một dòng:

```python
api_router.include_router(feedback_router, prefix="/tickets", tags=["feedback"])
```

**d. Test** — `backend/tests/api/test_danh_gia.py`, tối thiểu 6 ca:

1. Đánh giá ticket `CLOSED` → 201
2. Đánh giá ticket `IN_PROGRESS` → 422
3. Người khác đánh giá ticket không phải của mình → **404**
4. Đánh giá lần hai → 409
5. `score = 0` và `score = 6` → 422
6. Sửa trong 24 giờ → 200

> Test dựng ticket phải để ý ràng buộc `ck_tickets_assignee`: ticket khác `NEW` **bắt buộc có assignee**. Không gán là DB từ chối ngay, và thông báo lỗi rất khó đọc.

## Cách tự kiểm tra

```bash
cd backend && .venv-tools/bin/pytest tests/api/test_danh_gia.py -v
```

Rồi mở http://localhost:8000/docs, tìm nhóm `feedback`, bấm "Try it out" với một ticket đã đóng.

## Commit

```bash
git add backend/app/modules/feedback backend/app/api/v1/router.py backend/tests/api/test_danh_gia.py
git commit -m "feat(feedback): API danh gia sau xu ly (US-41)

Bang ticket_ratings da co san UNIQUE tren ticket_id va CHECK score 1..5,
nen tang service chi can dich ngoai le cho dung ma HTTP.

Chep assignee_id vao agent_id ngay luc danh gia thay vi join khi doc: bao
cao hai long phai phan anh nguoi thuc su xu ly luc do, ticket giao lai ve
sau khong duoc lam doi so lieu lich su.

Danh gia ticket cua nguoi khac tra 404 chu khong phai 403 (BR-09) — 403 la
gian tiep xac nhan ticket do co that."
```

## Hoàn thành khi

- [ ] 6 test pass
- [ ] Thấy nhóm `feedback` trong Swagger
- [ ] Đánh giá ticket người khác trả 404

---

# 4️⃣ Trần Quang Ngọc — Hai test phân quyền bắt buộc (US-06)

| | |
|---|---|
| **Nhánh** | `test/US-06-ma-tran-phan-quyen` |
| **File tạo mới** | `backend/tests/api/test_ma_tran_phan_quyen.py` |
| **File sửa** | không có |
| **File mẫu để copy** | `backend/tests/api/test_quyen_nghiep_vu_ticket.py` |
| **Thời gian** | ~3 giờ |
| **SP** | 3 |

## Bối cảnh

`docs/design/10-test-strategy.md` liệt kê hai test là **bắt buộc**, và cả hai đến giờ vẫn chưa ai viết. Đây là hai test có giá trị cao nhất trong dự án vì chúng bảo vệ **toàn bộ** API chứ không phải một endpoint.

Hôm nay chưa có gì ngăn một người thêm endpoint mới mà quên `Depends(get_current_user)`. Endpoint đó sẽ mở toang cho cả internet và **không test nào đỏ**.

Xung đột file: **bằng không** — bạn tạo một file mới hoàn toàn.

## Test 1 — Quét toàn bộ route

Ý tưởng: duyệt mọi route trong `app.routes`, gọi thử **không kèm token**, và khẳng định server trả 401 hoặc 403 — tuyệt đối không phải 200.

```python
CONG_KHAI = {
    "/health",
    "/api/v1/auth/login",
    "/api/v1/auth/register",
    "/api/v1/auth/refresh",
    "/docs", "/openapi.json", "/redoc",
}

def test_moi_endpoint_deu_doi_xac_thuc(client):
    """Endpoint mới quên Depends(get_current_user) sẽ bị bắt ở đây.

    Danh sách trắng phải viết TAY. Nếu ai đó thêm endpoint công khai mới,
    họ buộc phải sửa file này — và lúc sửa sẽ phải tự hỏi 'endpoint này
    công khai có đúng không?'. Đó chính là tác dụng của test.
    """
    for route in app.routes:
        duong_dan = getattr(route, "path", None)
        if duong_dan is None or duong_dan in CONG_KHAI:
            continue
        for method in sorted(getattr(route, "methods", set()) - {"HEAD", "OPTIONS"}):
            # Thay tham số đường dẫn bằng một UUID hợp lệ bất kỳ
            url = re.sub(r"\{[^}]+\}", str(uuid4()), duong_dan)
            r = client.request(method, url)
            assert r.status_code in (401, 403), (
                f"{method} {duong_dan} tra {r.status_code} khi KHONG co token"
            )
```

## Test 2 — Ma trận phân quyền

Nguồn sự thật: `docs/design/06-api-design.md` **§5 Ma trận phân quyền**. Mở tài liệu đó, chép bảng thành dữ liệu:

```python
# (method, đường dẫn, vai trò, mã trạng thái KHÔNG được phép nhận)
MA_TRAN = [
    ("GET",  "/api/v1/reports/overview",   UserRole.EMPLOYEE, 403),
    ("GET",  "/api/v1/reports/overview",   UserRole.IT_AGENT, 403),
    ("GET",  "/api/v1/users",              UserRole.EMPLOYEE, 403),
    ("POST", "/api/v1/kb/articles",        UserRole.EMPLOYEE, 403),
    # ... chép nốt phần còn lại từ §5
]

@pytest.mark.parametrize("method,path,role,expected", MA_TRAN)
def test_ma_tran_phan_quyen(client, make_user, login, method, path, role, expected):
    c = login(make_user(role=role))
    assert c.request(method, path).status_code == expected
```

> **Nếu code và tài liệu mâu thuẫn:** đừng tự sửa bên nào. Ghi lại vào phần mô tả PR và nhắn nhóm trưởng. Có thể tài liệu sai, cũng có thể code sai — người quyết định là chủ sở hữu module.

## Cách tự kiểm tra

```bash
cd backend && .venv-tools/bin/pytest tests/api/test_ma_tran_phan_quyen.py -v
```

**Kiểm tra test có thật sự bắt được lỗi không** (bước này quan trọng hơn việc test xanh): mở `backend/app/modules/reports/router.py`, tạm xoá dependency kiểm tra quyền của một endpoint, chạy lại test — **phải đỏ**. Rồi khôi phục lại.

> Test xanh mà không bắt được lỗi nào thì tệ hơn không có test: nó tạo cảm giác an toàn giả. Dự án này đã có bốn lỗi thật lọt qua một bộ test toàn xanh.

## Commit

```bash
git add backend/tests/api/test_ma_tran_phan_quyen.py
git commit -m "test(security): quet toan bo route va ma tran phan quyen (US-06)

Hai test bat buoc theo 10-test-strategy.md ma chua ai viet. Ca hai bao ve
toan bo API thay vi mot endpoint: them endpoint moi ma quen Depends(
get_current_user) se bi bat ngay, thay vi mo toang cho ca internet ma
khong test nao do.

Danh sach endpoint cong khai viet tay co chu dich — ai them endpoint cong
khai moi buoc phai sua file nay, va luc do se phai tu hoi lieu no cong
khai co dung khong.

Da kiem chung bang cach tam go dependency kiem quyen cua mot endpoint:
test do dung nhu mong doi."
```

## Hoàn thành khi

- [ ] Cả hai test pass
- [ ] Đã **chứng minh** test bắt được lỗi bằng cách tạm gỡ một guard
- [ ] Ghi lại trong PR những chỗ code lệch tài liệu (nếu có)

---

# 5️⃣ Nguyễn Văn Dũng — Trang Đăng ký + Đổi mật khẩu

| | |
|---|---|
| **Nhánh** | `feature/US-01-US-05-ui` |
| **File tạo mới** | `frontend/src/features/auth/RegisterPage.tsx`, `frontend/src/features/auth/ChangePasswordPage.tsx` |
| **File sửa** | `frontend/src/App.tsx` (2 dòng), `frontend/src/api/client.ts` hoặc thêm `api/auth.ts` |
| **File mẫu để copy** | `frontend/src/features/auth/LoginPage.tsx` ← **copy gần như nguyên xi** |
| **Thời gian** | ~3 giờ |
| **SP** | 5 |

## Bối cảnh

`POST /auth/register` và `POST /auth/change-password` đã chạy từ Sprint đầu, có test đầy đủ. Nhưng `features/auth/` chỉ có `LoginPage.tsx` — **không form nào gọi tới hai API đó**.

Người chấm mở ứng dụng lần đầu sẽ bấm "Đăng ký" trước cả khi đăng nhập. Không có trang đó là ấn tượng đầu tiên hỏng.

## Việc cần làm

**a. `RegisterPage.tsx`** — route `/register`, **không** bọc `ProtectedRoute`

Trường: `email`, `fullName`, `password`, `confirmPassword`, `departmentId` (dropdown lấy từ `GET /users/departments`).

Quy tắc mật khẩu do backend quy định — tối thiểu 8 ký tự, có chữ hoa, chữ thường và số. Kiểm tra ngay tại form để người dùng không phải bấm gửi mới biết sai.

Đăng ký thành công → chuyển sang `/login` kèm thông báo "Đăng ký thành công, mời đăng nhập".

Thêm link "Đã có tài khoản? Đăng nhập" và ở `LoginPage` thêm link ngược lại.

**b. `ChangePasswordPage.tsx`** — route `/change-password`, **có** bọc `ProtectedRoute`

Trường: `currentPassword`, `newPassword`, `confirmPassword`.

> ⚠️ **Đổi mật khẩu thành công sẽ thu hồi MỌI phiên, kể cả phiên đang dùng.** Backend cố tình làm vậy — người đổi mật khẩu thường vì nghi bị lộ, để phiên cũ sống tiếp thì việc đổi thành vô nghĩa. Vì thế sau khi thành công **phải xoá token trong `AuthProvider` và chuyển về `/login`** kèm thông báo "Đổi mật khẩu thành công, mời đăng nhập lại". Nếu quên bước này, người dùng sẽ thấy toàn bộ ứng dụng lỗi 401 và tưởng hệ thống hỏng.

Thêm lối vào trang này từ menu người dùng ở góc trên bên phải.

**c. Hiển thị lỗi từ backend**

Backend trả lỗi theo định dạng thống nhất — đọc `error.message` mà hiện, đừng hiện "Có lỗi xảy ra":

```json
{ "error": { "code": "VALIDATION_ERROR", "message": "..." } }
```

Riêng khi đổi mật khẩu, **403 nghĩa là mật khẩu hiện tại không đúng** (Lại Duy Đông đang sửa mã lỗi này trong cùng ngày — trước đó là 401). Xử lý cả hai để không phụ thuộc thứ tự merge.

## Cách tự kiểm tra

```bash
cd frontend && npm run dev
```

Mở http://localhost:5173/register khi **chưa đăng nhập** — phải vào được. Đăng ký một tài khoản mới, đăng nhập bằng chính tài khoản đó. Vào `/change-password`, đổi mật khẩu, xác nhận bị đá về `/login`, rồi đăng nhập bằng mật khẩu mới.

## Commit

```bash
git add frontend/src/features/auth frontend/src/App.tsx frontend/src/api
git commit -m "feat(auth): trang dang ky va doi mat khau (US-01, US-05)

Hai API da chay tu Sprint dau nhung khong co form nao goi toi.

Sau khi doi mat khau thanh cong phai xoa token va chuyen ve /login: backend
thu hoi MOI phien ke ca phien dang dung, nen neu giu nguyen token thi toan
bo ung dung se loi 401 va nguoi dung tuong he thong hong.

Xu ly ca 401 lan 403 cho truong hop sai mat khau hien tai de khong phu
thuoc thu tu merge voi nhanh fix/US-05-ma-loi-va-audit."
```

## Hoàn thành khi

- [ ] Đăng ký được tài khoản mới và đăng nhập bằng nó
- [ ] Đổi mật khẩu xong bị đưa về `/login` đúng như mô tả
- [ ] `npm run typecheck && npm run build` sạch

---

# 6️⃣ Chu Quang Vũ — Sửa và xoá bình luận (US-15)

| | |
|---|---|
| **Nhánh** | `feature/US-15-sua-xoa-binh-luan` |
| **File sửa** | `backend/app/modules/tickets/{router,service,schemas,repository}.py` |
| **File tạo mới** | `backend/tests/api/test_binh_luan_sua_xoa.py` |
| **Thời gian** | ~3 giờ |
| **SP** | 3 |

## Bối cảnh

Hiện chỉ có `GET` và `POST /tickets/{id}/comments`. Viết sai một chữ là phải sống chung với nó vĩnh viễn.

> ⚠️ **Bạn là người duy nhất được sửa `tickets/service.py` hôm nay.** Nhóm trưởng đã dời việc `paused_seconds` sang lượt sau để tránh giẫm chân. Nếu cần đụng file ngoài module `tickets/`, nhắn trước.

## Việc cần làm

**a. Hai endpoint mới**

```
PATCH  /tickets/{ticket_id}/comments/{comment_id}
DELETE /tickets/{ticket_id}/comments/{comment_id}
```

**b. Quy tắc nghiệp vụ**

| Quy tắc | Mã trả về |
|---|---|
| Chỉ **tác giả** sửa được bình luận của mình | người khác → 403 |
| Admin xoá được bình luận bất kỳ | |
| Bình luận không thuộc ticket trong URL | 404 |
| Người không có quyền xem ticket | **404** (BR-09, không tiết lộ sự tồn tại) |
| Ticket đã `CLOSED` | 422 — đóng rồi thì lịch sử trao đổi phải đứng yên |

**c. Xoá là xoá MỀM (BR-16)**

Dự án **không hard delete**. Thêm cột `deleted_at` — nhưng **đừng tự chạy `alembic revision`**, hôm nay chỉ Nguyễn Đăng Trường được tạo migration. Hai lựa chọn:

1. Nhắn Trường thêm cột vào revision của anh ấy, rồi bạn rebase lên; **hoặc**
2. Làm trước phần `PATCH`, mở PR, để `DELETE` sang lượt sau.

Lựa chọn 2 an toàn hơn nếu bạn muốn xong trong ngày.

**d. Ghi vết sửa đổi**

Bình luận đã sửa phải nhìn thấy được là đã sửa — thêm `edited_at` và trả về trong `CommentResponse` để giao diện hiện "(đã chỉnh sửa)". Sửa lặng lẽ trong một hệ thống ticket là chuyện dễ gây tranh cãi về sau.

## Test tối thiểu

1. Tác giả sửa bình luận của mình → 200, `editedAt` khác `null`
2. Người khác sửa → 403
3. Admin xoá bình luận người khác → 204
4. Sửa bình luận trên ticket `CLOSED` → 422
5. `comment_id` không thuộc `ticket_id` trong URL → 404
6. Người không được xem ticket → 404 (**không phải 403**)

## Cách tự kiểm tra

```bash
cd backend && .venv-tools/bin/pytest tests/api/test_binh_luan_sua_xoa.py tests/api/test_tickets.py -v
```

Chạy cả `test_tickets.py` để chắc chắn không làm hỏng phần đã có.

## Commit

```bash
git add backend/app/modules/tickets backend/tests/api/test_binh_luan_sua_xoa.py
git commit -m "feat(tickets): sua va xoa binh luan (US-15)

Truoc gio chi co GET va POST — viet sai mot chu la phai song chung vinh vien.

Chi tac gia sua duoc, admin xoa duoc bat ky. Ticket da CLOSED thi khoa: dong
roi thi lich su trao doi phai dung yen.

Them edited_at de giao dien hien '(da chinh sua)'. Sua lang le trong mot he
thong ticket la chuyen de gay tranh cai ve sau.

Binh luan tren ticket khong duoc xem tra 404 chu khong phai 403 (BR-09)."
```

## Hoàn thành khi

- [ ] 6 test pass (hoặc 4 nếu tạm để `DELETE` lại)
- [ ] `pytest tests/api/test_tickets.py` vẫn xanh

---

# 7️⃣ Nguyễn Tiến Lưỡng — Báo cáo câu hỏi chatbot trả lời kém (US-27)

| | |
|---|---|
| **Nhánh** | `feature/US-27-cau-hoi-tra-loi-kem` |
| **File sửa** | `backend/app/modules/reports/{router,schemas}.py` |
| **File tạo mới** | `backend/app/modules/reports/unanswered.py`, `backend/tests/integration/test_cau_hoi_khong_tra_loi_duoc.py` |
| **File mẫu để copy** | `backend/app/modules/reports/dashboard.py` |
| **Thời gian** | ~3 giờ |
| **SP** | 5 |

## Bối cảnh

Cột `chat_messages.no_context_found` đã được ghi đúng từ ngày đầu, nhưng **chưa ai đọc nó**. Mỗi lần chatbot trả lời "tôi không tìm thấy tài liệu phù hợp", hệ thống đang lặng lẽ ghi lại một chỗ thiếu tài liệu — và không ai nhìn.

Báo cáo này khép vòng lặp cải thiện chatbot: **câu hỏi không trả lời được → biết thiếu tài liệu gì → viết bài → chatbot trả lời được**. Không có nó thì việc bổ sung tài liệu chỉ là đoán.

## Việc cần làm

**a. Endpoint mới**

```
GET /reports/unanswered-questions?from=&to=&page=&pageSize=
```

Quyền: `ADMIN` **và** `IT_AGENT` (giống `/reports/ai-accuracy` — Agent chính là người viết tài liệu).

**b. Truy vấn**

Lấy các `chat_messages` có `role = 'USER'` mà **tin nhắn ngay sau đó** của trợ lý có `no_context_found = true`. Dùng `LEAD(...) OVER (PARTITION BY session_id ORDER BY created_at)` — xem `dashboard.py::_pause_seconds_subquery()` đã dùng đúng kỹ thuật này.

Gom nhóm câu hỏi **giống nhau sau khi chuẩn hoá** (bỏ dấu, hạ chữ thường). Đừng viết hàm bỏ dấu bằng Python — database **đã có sẵn** hàm `immutable_unaccent(text)` (tạo trong `migrations/versions/0004_search_triggers.py`, đang được dùng cho tìm kiếm ticket). Gom nhóm ngay trong SQL:

```sql
GROUP BY lower(immutable_unaccent(m.content))
```

Làm ở tầng SQL vừa nhanh hơn vừa tránh phải kéo dữ liệu về Python để gom.

Trả về:

```json
{ "data": [ { "question": "...", "count": 12,
              "lastAskedAt": "...", "sampleSessionId": "..." } ],
  "pagination": { ... } }
```

Sắp xếp theo `count` giảm dần — chủ đề nào thiếu tài liệu nhất nằm trên cùng.

**c. Bộ nhớ đệm**

Dùng lại `reports/cache.py`, TTL 5 phút.

> ⚠️ **Bắt buộc dùng `window_key()`.** Bản đầu tiên của cache trong dự án này ghép thẳng `datetime.now().isoformat()` vào khoá — chính xác tới micro giây, nên **mỗi request tạo một khoá mới và không lần nào trúng**. Nhìn từ ngoài y hệt như cache đang hoạt động. `window_key()` làm tròn thời gian xuống bội số TTL để khoá lặp lại được.

**d. Phần AC xin phép cắt bớt**

AC gốc của US-27 còn hai ý: gom nhóm theo **độ tương đồng embedding**, và nút "Tạo bài KB từ câu hỏi này". Hôm nay **chỉ làm gom nhóm theo văn bản chuẩn hoá**. Hai ý còn lại ghi vào PR như việc tiếp theo — gom theo embedding cần bàn thêm về ngưỡng, và nút tạo bài phụ thuộc trang KB mà Nguyễn Văn Quang đang làm song song.

## Test tối thiểu

1. Dựng 3 phiên chat, 2 phiên có câu bị `no_context_found` → endpoint trả đúng 2 câu
2. Hai câu hỏi khác nhau về dấu ("cai dat vpn" và "cài đặt VPN") → gom thành **một** nhóm, `count = 2`
3. `EMPLOYEE` gọi → 403
4. Lọc theo khoảng ngày trả đúng tập con

## Cách tự kiểm tra

```bash
cd backend && .venv-tools/bin/pytest tests/integration/test_cau_hoi_khong_tra_loi_duoc.py -v
```

Rồi thử thật: mở `/chat`, hỏi một câu chắc chắn ngoài phạm vi ("giá vàng hôm nay bao nhiêu"), chatbot sẽ từ chối. Gọi endpoint mới — câu đó phải xuất hiện.

## Commit

```bash
git add backend/app/modules/reports backend/tests/integration/test_cau_hoi_khong_tra_loi_duoc.py
git commit -m "feat(reports): bao cao cau hoi chatbot khong tra loi duoc (US-27)

Cot chat_messages.no_context_found duoc ghi dung tu ngay dau nhung chua ai
doc. Moi lan chatbot tu choi la mot cho thieu tai lieu duoc ghi lai lang le.

Bao cao nay khep vong lap cai thien RAG: cau hoi khong tra loi duoc → biet
thieu tai lieu gi → viet bai → chatbot tra loi duoc. Khong co no thi viec
bo sung tai lieu chi la doan.

Gom nhom theo van ban chuan hoa (bo dau, ha chu thuong) nen 'cai dat vpn'
va 'cai dat VPN' vao cung mot nhom. Gom theo do tuong dong embedding de
lai lan sau — can ban them ve nguong."
```

## Hoàn thành khi

- [ ] 4 test pass
- [ ] Câu hỏi ngoài phạm vi hỏi qua `/chat` xuất hiện trong báo cáo
- [ ] `EMPLOYEE` gọi bị 403

---

# 8️⃣ Lại Duy Đông — Sửa mã lỗi đổi mật khẩu và ghi audit (US-05)

| | |
|---|---|
| **Nhánh** | `fix/US-05-ma-loi-va-audit` |
| **File sửa** | `backend/app/modules/auth/service.py` |
| **File sửa thêm** | `backend/tests/api/test_auth.py` |
| **Thời gian** | ~1,5 giờ |
| **SP** | 2 |

## Bối cảnh

Task nhỏ nhất trong ngày — hợp để lấy lại đà.

`AuthService.change_password` hiện như sau (`service.py:214`):

```python
def change_password(self, user: User, current_password: str, new_password: str) -> int:
    if not verify_password(current_password, user.password_hash):
        raise InvalidCredentialsError("Mật khẩu hiện tại không đúng")   # ← trả 401
```

Hai vấn đề:

**Sai mã lỗi.** `InvalidCredentialsError` → **401**, nghĩa là "bạn chưa đăng nhập". Nhưng người gọi API này **đã đăng nhập rồi** — token của họ hợp lệ, chỉ mật khẩu cũ gõ sai. Đúng ra phải là **403**.

Đây không phải chuyện câu chữ: frontend thường cài sẵn interceptor "gặp 401 thì xoá token và đá về trang đăng nhập". Nghĩa là **gõ nhầm mật khẩu cũ một lần là bị đăng xuất**, và người dùng không hiểu vì sao.

**Không ghi vết thất bại.** Hàm này có `logger.info` khi đổi **thành công**, nhưng khi **thất bại** thì im lặng. Đó mới là sự kiện đáng ghi: nhiều lần thất bại liên tiếp trên một tài khoản là dấu hiệu có người đang dò mật khẩu.

## Việc cần làm

**a. Đổi ngoại lệ**

```python
from app.core.exceptions import ForbiddenError

if not verify_password(current_password, user.password_hash):
    logger.warning(
        "doi mat khau that bai: sai mat khau hien tai",
        extra={"extra_fields": {"user_id": str(user.id), "email": user.email}},
    )
    raise ForbiddenError("Mật khẩu hiện tại không đúng")
```

> ⚠️ **Tuyệt đối không ghi mật khẩu — cũ hay mới — vào log**, kể cả một phần. Log được gom về nơi lưu trữ tập trung và nhiều người đọc được.

**b. Sửa test cũ**

Trong `tests/api/test_auth.py` có test đang khẳng định `401`. Nó **phải được sửa thành `403`**, không phải xoá đi.

**c. Thêm hai test**

1. Sai mật khẩu hiện tại → **403**, và mã lỗi trong body là `FORBIDDEN`
2. Sai mật khẩu hiện tại → token đang dùng **vẫn còn hiệu lực** (gọi `GET /users/me` ngay sau đó vẫn 200)

Test thứ hai chính là bằng chứng cho vấn đề đã nêu ở trên.

## Cách tự kiểm tra

```bash
cd backend && .venv-tools/bin/pytest tests/api/test_auth.py -v
```

## Commit

```bash
git add backend/app/modules/auth/service.py backend/tests/api/test_auth.py
git commit -m "fix(auth): sai mat khau hien tai tra 403 thay vi 401, ghi audit (US-05)

401 nghia la 'chua dang nhap', nhung nguoi goi API nay DA dang nhap — token
hop le, chi mat khau cu go sai. Frontend thuong cai interceptor 'gap 401 thi
xoa token va da ve trang dang nhap', nen go nham mot lan la bi dang xuat va
nguoi dung khong hieu vi sao.

Ham nay dang ghi log khi doi THANH CONG nhung im lang khi that bai. Nhieu
lan that bai lien tiep tren mot tai khoan la dau hieu co nguoi dang do mat
khau — do moi la su kien dang ghi.

Them test khang dinh token van con hieu luc sau khi go sai mat khau cu."
```

## Hoàn thành khi

- [ ] `pytest tests/api/test_auth.py` xanh
- [ ] Gõ sai mật khẩu cũ trả 403 và **không** bị đăng xuất

---

# 📊 PHẦN 3 — SAU KHI XONG

## Tự đánh dấu tiến độ

Xong task nào thì tick vào bảng ở [`docs/design/01-requirement-analysis.md`](../docs/design/01-requirement-analysis.md) §10 trong chính PR của mình.

## Việc nhóm trưởng làm song song

| Việc | Vì sao không giao ra |
|---|---|
| ~~Merge `fix/p0-nghiep-vu`~~ | ✅ **Xong** — PR #19, #20, #22 |
| `paused_seconds` — SLA không tạm dừng khi chờ nhân viên | Đụng sâu vào `tickets/service.py`, mà Chu Quang Vũ đang sửa trong đó |
| Đo lại chất lượng phân loại sau khi đổi prompt | Cần API key trả phí, giữ ở một đầu mối |

## Còn thiếu sau ngày hôm nay

Ghi ra đây để cả nhóm biết bức tranh đầy đủ, **chưa giao hôm nay**:

| | SP | Ghi chú |
|---|---|---|
| US-09 Đính kèm file | 5 | MinIO đã có sẵn trong `docker-compose.yml`, chưa nối |
| US-42, US-43 Tổng hợp điểm hài lòng | 5 | Cần US-41 xong trước |
| US-26 Đánh dấu câu trả lời hữu ích | 3 | |
| US-22 Giao diện độ chính xác AI | 5 | API `/reports/ai-accuracy` đã chạy |
| US-08 Idempotency-Key | 2 | Bảng `idempotency_keys` đã có, chưa dùng |
| Triển khai staging | — | **Chặn Definition of Done của mọi story** |

## Nhắc lại ba điều dễ quên nhất

1. **`git pull origin develop` trước khi tạo nhánh** — bỏ qua bước này là nguyên nhân số một gây conflict.
2. **Chạy `worker` và `beat`**, nếu không ticket sẽ không được phân loại và bạn sẽ đi tìm một lỗi không tồn tại.
3. **Chỉ Nguyễn Đăng Trường tạo migration hôm nay.**
